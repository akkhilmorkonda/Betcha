# Where the API lives. Inlined into the bundle at BUILD time, not read at run time.
#
# On a physical device "localhost" is the phone, not your machine. Use your
# LAN IP (run `next dev -H 0.0.0.0`) or a tunnel host — one exact host, never
# a wildcard.
EXPO_PUBLIC_API_URL="http://localhost:3000"
